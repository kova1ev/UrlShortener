@page "/counter"
@inject HttpClient Http

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="Execute">Click me</button>


@if (JsonString != null)
{
    <p> @JsonString </p>
}


@code {
    private int currentCount = 0;

    private string JsonString { get; set; } = "hello";

    private void IncrementCount()
    {
        currentCount++;
    }

    protected override async Task OnInitializedAsync()
    {
        // var jsonString = await Http.GetStringAsync("api/link");// GetFromJsonAsync<string>("api/link");
        //return base.OnInitializedAsync();
    }

    private async Task Execute()
    {
        var jsonString = "None";
#if DEBUG
     jsonString  = await Http.GetStringAsync("api/link");// GetFromJsonAsync<string>("api/link");
#endif
        Console.WriteLine(jsonString);

    }
}
